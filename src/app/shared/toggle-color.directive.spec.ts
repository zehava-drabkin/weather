import { ToggleColorDirective } from './toggle-color.directive';

describe('ToggleColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleColorDirective();
    expect(directive).toBeTruthy();
  });
});
